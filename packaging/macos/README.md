# macOS packaging status

This directory is a packaging scaffold, not a finished installer. The supplied
LaunchDaemon plist shows the intended service boundary. It is not signed,
notarized, installed automatically, or connected to an utun/Network Extension
adapter. See `docs/macos-client.md` for the remaining requirements.
