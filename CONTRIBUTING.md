# Contributing

Open an issue for substantial design/protocol changes. Keep changes focused; add
tests and update docs/OpenAPI in the same PR. Run `gofmt`, `go test ./...`,
`go test -race ./...` and `go vet ./...`. Describe security, migration and
compatibility impact. Do not report vulnerabilities publicly; follow SECURITY.md.
Contributions are accepted under Apache-2.0.
