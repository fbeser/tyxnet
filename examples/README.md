# Examples

Runnable development configuration lives in `../configs/server.yaml` and
`../configs/client.yaml`. Never copy example passwords, tokens or TLS-disabled
settings into an internet-facing deployment.
